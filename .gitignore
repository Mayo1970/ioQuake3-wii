# Build artifacts
build/
*.o
*.d
*.a
*.elf
*.dol
*.map
*.gch

# Vendored prebuilt OpenGX library is tracked source — keep it in the repo
!libs/opengx/lib/libopengx.a

# WiiU gamepad stuff
!libs/wiidrc/lib/libwiidrc.a


# Copyrighted Quake III Arena data — never commit
*.pk3
qkey

# CLASSIC's own bundled pak is our content, not retail data -- keep it tracked
!fixes/baseq3/*.pk3

# Legacy / retired tooling
legacy/

# AI tooling
.claude/
CLAUDE.md

# Misc
build-debug.log
